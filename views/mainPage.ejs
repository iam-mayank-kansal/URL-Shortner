<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: black;
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            background-color: #1a1a1a;
            padding: 40px;
            border-radius: 10px;
            max-width: 700px;
            text-align: center;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .container:hover {
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.8);
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            color: #0ccc6c;
        }

        .header p {
            font-size: 1.2em;
            margin-bottom: 30px;
            color: #cccccc;
        }

        .url-form {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border: 1px solid #444;
            border-radius: 5px;
            padding: 10px;
            background-color: #333;
        }

        .url-form input {
            flex: 1;
            border: none;
            padding: 12px;
            font-size: 1em;
            color: #fff;
            background-color: #444;
            outline: none;
            border-radius: 5px 0 0 5px;
        }

        .url-form button {
            background-color: #0ccc6c;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 0 5px 5px 0;
            cursor: pointer;
            font-size: 1em;
            font-weight: 500;
            transition: background-color 0.3s;
        }

        .url-form button:hover {
            background-color: #0baa5d;
        }

        .output-section {
            margin-top: 20px;
            text-align: center;
        }

        .output-section p {
            font-size: 1em;
            color: #cccccc;
        }

        .short-url {
            margin-top: 10px;
            background-color: #444;
            padding: 10px;
            border-radius: 5px;
            font-weight: 600;
            font-size: 1.2em;
            color: #0ccc6c;
            display: none;
            position: relative;
        }

        .copy-btn {
            background-color: #0ccc6c;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.9em;
            margin-top: 10px;
            transition: background-color 0.3s;
            display: none;
        }

        .copy-btn:hover {
            background-color: #0baa5d;
        }

        .short-url.visible {
            display: block;
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Shorten Your URL</h1>
            <p>Enter your long URL and get a short, simple one.</p>
        </div>

        <form class="url-form" method="POST" >
            <input type="text" id="long-url" name="longUrl" placeholder="Paste your long URL here..." required>
            <button type="submit">Shorten URL</button>
        </form>

        <div class="output-section">
            <p>Your shortened URL will appear here:</p>
            <div class="short-url" id="short-url"></div>
        </div>
    </div>

    <script>

    </script>
</body>

</html>